file dt_event.py:
    take UniqueTime and RecurringTime as parameter instead of 4 ints on GetUniqueTimes and GetRecurringTimes

file "dt_renderer.py":
    class TableRenderer - Create window, render Events
    class TableRendererSettings


file "dt_main.py":
    Parse config,
    extract events
    create renderer
    Monitor file changes to config file, reparse if changed
    once per day: clean config file.
